<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web Color Compiler</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <header>
        <h1>Web Color Compiler</h1>
    </header>
    <main>
        <form action="/compile" method="POST">
            <textarea name="htmlCode" placeholder="HTML Code" rows="10" cols="30"><%= htmlCode %></textarea>
            <textarea name="cssCode" placeholder="CSS Code" rows="10" cols="30"><%= cssCode %></textarea>
            <textarea name="jsCode" placeholder="JavaScript Code" rows="10" cols="30"><%= jsCode %></textarea>
            <button type="submit">Compile</button>
        </form>
        <div class="output">
            <h2>Output</h2>
            <iframe id="outputFrame" srcdoc="<%= htmlCode %>" style="width:100%; height:400px; border:none;"></iframe>
        </div>
        <script src="/scripts/main.js"></script>
    </main>
</body>
</html>
